# Core
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
# Deep learning (install the build matching your CUDA, e.g. from pytorch.org)
torch>=2.1
torchvision>=0.16
# Federated framework (simulation extra pulls Ray)
flwr[simulation]>=1.7,<2.0
# Optional: faithful attributions
captum>=0.7
